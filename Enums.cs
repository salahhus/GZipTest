using System;
using System.Collections.Generic;
using System.Text;

namespace GZipTest
{
    enum OperationEnum
    {
        // Compress mode
        compress,
        // Decompress mode
        decompress
    }
}
